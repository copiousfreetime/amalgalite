For future releases
===================
* raise exception if another sqlite3 lib is found to already be loaded, or namespace the sqlite3 functions somehow
* amalgalite command line tool
* active record driver
* data mapper driver
* sequel driver
* authorizers
* busy timeouts
* progress handler
* loading of extensions
* utf-16 integration
* create_collation 
* global temp directory setting
* create_function
* full text search
* virtual file system
* encryption key support
* convert type dependency to just use 'call'
* use ruby's ALLOC_N and hook into sqlite3_mem_methods
